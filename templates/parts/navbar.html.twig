<nav class="navbar navbar-expand-lg shadow-sm navbar-light bg-light">
  <div class="container-fluid">
    <a class="navbar-brand" href="{{path('app_welcome')}}">Automatron 🤖</a>
    <ul class="navbar-nav ms-auto mb-2 mb-lg-0">
      <li class="nav-item dropdown">
        <a class="nav-link" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
          <i class="bi bi-person fs-3 {% if is_granted('IS_AUTHENTICATED_FULLY') %}text-primary{% endif %}"></i>
        </a>
        <ul class="dropdown-menu dropdown-menu-end list-style-none">
            {% if is_granted('IS_AUTHENTICATED_FULLY') %}
                <li><p class="dropdown-item mb-0">Compte : {{ app.user.email }}</p></li>
                <li><hr class="dropdown-divider"></li>
                <li><a class="dropdown-item" href="{{path('app_logout')}}">Se déconnecter</a></li>
            {% else %}
              <li><a class="dropdown-item" href="{{path('app_login')}}">Se connecter</a></li>
              <li><a class="dropdown-item" href="{{path('app_register')}}">S'inscrire</a></li>
            {% endif %}
        </ul>
      </li>
    </ul>
  </div>
</nav>